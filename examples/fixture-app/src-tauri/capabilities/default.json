{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default fixture permissions for tauri-agent testing.",
  "windows": ["main"],
  "permissions": ["core:default", "agent:default"]
}
